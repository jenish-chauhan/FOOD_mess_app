cat > .env <<'EOF'
MYSQL_ROOT_PASSWORD=ChangeThisRootPassw0rd!
DB_USER=trackserve
DB_PASSWORD=StrongDbPassw0rd!
DB_NAME=track_serve
DB_PORT=3306
GUNICORN_WORKERS=3
EOF

//for the memory use 
free -h

//login to dokcer image 
docker exec -it track_serve_db bash

//access the databse 
mysql -u trackserve -p track_serve

//show the table 
show tables;
 SELECT * FROM grocery_vegetable_management;